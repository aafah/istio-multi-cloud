apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: deny-all
  namespace: istio-system
spec: {}
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: "allow-for-two"
  namespace: appspace
spec:
  selector:
    matchLabels:
      app: api-two
  rules:
#  - from:
#    - source:
#        principals: ["cluster.local/ns/appspace/svc/api-db-service"]
#    to:
#    - operation:
#        paths: ["/updates/*"]
#        methods: ["DELETE"]
#  - from:
#    - source:
#        principals: ["cluster.local/ns/istio-system/sa/istio-ingressgateway"]
#    to:
#    - operation:
#        paths: ["/updates/*"]
#        methods: ["GET", "POST"]
#  - from:
#    - source:
#        principals: ["cluster.local/ns/appspace/sa/probe-acc"]
#    to:
#    - operation:
#        methods: ["DELETE"]
---
apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: "allow-for-gate"
  namespace: appspace
spec:
  selector:
    matchLabels:
      app: react-fend
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/istio-system/sa/istio-ingressgateway-service-account"]
    to:
    - operation:
        methods: ["GET"]